# Passwords para postgres-data — NUNCA commitear el .env real
# Generar con: openssl rand -base64 24

POSTGRES_SUPERUSER_PASSWORD=<generar: openssl rand -base64 24>
ERP_USER_PASSWORD=<generar>
ANALYTICS_ETL_READER_PASSWORD=<generar>
BRAIN_USER_PASSWORD=<generar>
BRAIN_READER_PASSWORD=<generar>
