# Metabase env vars — copy a .env y rellenar desde Bitwarden
ANALYTICS_DB_USER=analytics_user
ANALYTICS_DB_PASSWORD=<bitwarden:postgres-analytics-password>

# Las dashboards exportados van a analytics/dashboards/*.json (versionados).
