# Vtiger env vars — copy a .env y rellenar desde Bitwarden
VTIGER_DB_NAME=livskin_db
VTIGER_DB_USER=livskin
VTIGER_DB_PASSWORD=<bitwarden:vtiger-db-password>
VTIGER_DB_ROOT_PASSWORD=<bitwarden:vtiger-db-root-password>

# IMPORTANTE: actualmente las passwords son TODAS literal "livskin" — TODO
# rotar a passwords reales en Bloque 0.7 (DR drill recovery procedure).
