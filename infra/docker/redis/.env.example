# Redis VPS2 — Sprint 1.1
# Copiar a .env (gitignored) en /home/livskin/apps/redis/

# Password fuerte (mínimo 32 chars). Generar con:
#   openssl rand -base64 32 | tr -d '\n='
REDIS_PASSWORD=changeme_use_openssl_rand_base64
